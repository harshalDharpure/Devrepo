# Copy to frontend/.env.local ΓÇö do not commit .env.local

# Local backend (agent-backend branch, dev only)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Production ΓÇö Cloud Run backend URL from agent-backend deploy
NEXT_PUBLIC_API_URL=https://your-backend-xxxxx.run.app
